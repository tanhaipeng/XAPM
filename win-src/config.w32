// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("xapm", "for xapm support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("xapm", "enable xapm support", "no");

if (PHP_XAPM != "no") {
	EXTENSION("xapm", "xapm.c");
}

